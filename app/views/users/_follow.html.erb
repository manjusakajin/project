<%= form_for @relationship, remote: true do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <div><%= hidden_field_tag :user_id, @user.id %></div>
  <%= f.submit t("follow.title"), class: "btn btn-primary" %>
<% end %>
