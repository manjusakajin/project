<%= form_for @relationship,
  html: {method: :delete}, remote: true do |f| %>
  <div><%= hidden_field_tag :user_id, @user.id %></div>
  <%= f.submit t("follow.unfollow"), class: "btn btn-primary" %>
<% end %>
